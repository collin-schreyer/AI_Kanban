services:
  - type: web
    name: ai-kanban
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    disk:
      name: kanban-data
      mountPath: /data
      sizeGB: 1
